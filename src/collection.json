{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "injectable": {
      "aliases": ["ij"],
      "description": "Creates an injectable class.",
      "factory": "./injectable/index",
      "schema": "./injectable/schema.json"
    },
    "interceptor": {
      "aliases": ["ic"],
      "description": "Creates an interceptor.",
      "factory": "./interceptor/index",
      "schema": "./interceptor/schema.json"
    }
  }
}
